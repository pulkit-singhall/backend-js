// this file contains all the constants used in the project

const DB_NAME = "project_database";
const limit = "20kb";
const cookieOptions = {
    httpOnly: true,
    secure: true,
};

export {
    DB_NAME,
    limit,
    cookieOptions,
}
